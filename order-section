.order-section {
  padding: 5rem 1.25rem;
  background: linear-gradient(180deg, #fffaf7 0%, #ffffff 100%);
}

.order-wrap {
  max-width: 1100px;
  margin: 0 auto;
}

.order-form {
  display: grid;
  gap: 1.25rem;
}

.order-card,
.order-summary {
  background: #ffffff;
  border: 1px solid rgba(40, 30, 60, 0.12);
  border-radius: 22px;
  padding: 1.5rem;
  box-shadow: 0 12px 35px rgba(40, 30, 60, 0.08);
}

.order-card legend {
  font-size: 1.25rem;
  font-weight: 800;
  color: #2b2342;
  margin-bottom: 1rem;
}

.gift-option {
  display: grid;
  grid-template-columns: auto 1fr auto;
  gap: 1rem;
  align-items: center;
  padding: 1.1rem;
  border: 2px solid rgba(40, 30, 60, 0.12);
  border-radius: 18px;
  cursor: pointer;
  margin-bottom: 1rem;
  transition: 0.2s ease;
}

.gift-option:hover,
.gift-option:has(input:checked) {
  border-color: #ff8a3d;
  background: #fff4ed;
  transform: translateY(-2px);
}

.gift-content strong {
  display: block;
  font-size: 1.05rem;
  color: #2b2342;
  margin-bottom: 0.25rem;
}

.gift-content small {
  display: block;
  color: #60586f;
  line-height: 1.4;
}

.gift-price {
  font-size: 1.15rem;
  font-weight: 800;
  color: #2b2342;
}

.featured-gift {
  border-color: rgba(255, 138, 61, 0.45);
}

.form-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 1rem;
}

.order-form label {
  display: grid;
  gap: 0.4rem;
  color: #2b2342;
  font-weight: 700;
}

.order-form input,
.order-form select,
.order-form textarea {
  width: 100%;
  border: 1px solid rgba(40, 30, 60, 0.18);
  border-radius: 14px;
  padding: 0.85rem 0.95rem;
  font: inherit;
  background: #ffffff;
}

.order-form textarea {
  resize: vertical;
}

.conditional-section {
  display: none;
}

.conditional-section.show {
  display: block;
}

.mailing-choice {
  margin-top: 1rem;
  padding: 1rem;
  border-radius: 16px;
  background: #f8f5ff;
}

.mailing-choice p {
  margin: 0 0 0.75rem;
  font-weight: 800;
  color: #2b2342;
}

.mailing-choice label,
.terms-check {
  display: flex;
  align-items: center;
  gap: 0.6rem;
  font-weight: 600;
}

.mailing-choice input,
.terms-check input,
.gift-option input {
  width: auto;
}

.summary-line,
.summary-total {
  display: flex;
  justify-content: space-between;
  gap: 1rem;
  padding: 0.8rem 0;
  border-bottom: 1px solid rgba(40, 30, 60, 0.12);
}

.summary-total {
  font-size: 1.2rem;
  border-bottom: none;
}

.summary-note {
  color: #60586f;
}

.pay-button,
.submit-order {
  display: inline-flex;
  justify-content: center;
  align-items: center;
  border: 0;
  border-radius: 999px;
  padding: 0.9rem 1.4rem;
  font-weight: 800;
  text-decoration: none;
  background: #ff7a2f;
  color: #ffffff;
  cursor: pointer;
}

.pay-button.disabled {
  opacity: 0.5;
  pointer-events: none;
}

.submit-order {
  justify-self: start;
  font-size: 1rem;
}

@media (max-width: 760px) {
  .form-grid,
  .gift-option {
    grid-template-columns: 1fr;
  }

  .gift-price {
    justify-self: start;
  }
}
